# atoi_atof_userdefinedfunctons_in_c
implementation of atoi() and atof() in C to convert numeric strings into integer and floating-point values. Covers string traversal, ASCII-to-digit conversion, sign handling, decimal-point processing, fractional values, and input validation while understanding the internal conversion logic.
